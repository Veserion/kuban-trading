import React from "react";
import styled from "@emotion/styled";

type TProps = {
    selectedFert: string
}

export default class ShopPage extends React.Component<TProps> {
    render() {
        return <Root>

        </Root>
    }
}

const Root = styled.div`

`